// This component has been replaced by EmblaDots
// Keeping this file as a placeholder to avoid import errors in case there are other files still importing it
// Will be fully removed in a future cleanup

/* eslint-disable */
import React from "react";

interface ServiceIndicatorsProps {
  totalCount: number;
  activeIndex: number;
  onClick: (index: number) => void;
}

export function ServiceIndicators(_props: ServiceIndicatorsProps) {
  return null; // Return null to render nothing since this component is deprecated
}
/* eslint-enable */
